import 'package:flutter/material.dart';

class ScreenTransferMoney extends StatelessWidget {
  const ScreenTransferMoney({Key? key, }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
