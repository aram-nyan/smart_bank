import 'package:flutter/material.dart';

class ScreenConfirmPasscode extends StatelessWidget {
  const ScreenConfirmPasscode({Key? key, }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
