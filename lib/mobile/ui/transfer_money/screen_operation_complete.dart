import 'package:flutter/material.dart';

class ScreenOperationComplete extends StatelessWidget {
  const ScreenOperationComplete({Key? key, }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
