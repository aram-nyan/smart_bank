import 'package:flutter/material.dart';

class ScreenTopUpSplash extends StatelessWidget {
  const ScreenTopUpSplash({Key? key, }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
