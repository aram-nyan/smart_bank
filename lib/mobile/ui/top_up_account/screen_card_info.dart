import 'package:flutter/material.dart';

class ScreenCardInfo extends StatelessWidget {
  const ScreenCardInfo({Key? key, }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
