import 'package:flutter/material.dart';

class ScreenDebitOrCredit extends StatelessWidget {
  const ScreenDebitOrCredit({Key? key, }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
