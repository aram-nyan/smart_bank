import 'package:flutter/material.dart';

class ScreenCardName extends StatelessWidget {
  const ScreenCardName({Key? key, }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
