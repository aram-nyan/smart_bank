import 'package:flutter/material.dart';

class ScreenCardReadySplash extends StatelessWidget {
  const ScreenCardReadySplash({Key? key, }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
