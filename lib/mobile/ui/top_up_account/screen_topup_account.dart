import 'package:flutter/material.dart';

class ScreenTopUpAccount extends StatelessWidget {
  const ScreenTopUpAccount({Key? key, }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
