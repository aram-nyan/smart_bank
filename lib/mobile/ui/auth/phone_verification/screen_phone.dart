import 'package:flutter/material.dart';

class ScreenPhone extends StatelessWidget {
  const ScreenPhone({Key? key, }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
