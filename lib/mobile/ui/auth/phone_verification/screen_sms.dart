import 'package:flutter/material.dart';

class ScreenSms extends StatelessWidget {
  const ScreenSms({Key? key, }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
