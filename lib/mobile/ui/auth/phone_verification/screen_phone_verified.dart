import 'package:flutter/material.dart';

class ScreenPhoneVerified extends StatelessWidget {
  const ScreenPhoneVerified({Key? key, }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
