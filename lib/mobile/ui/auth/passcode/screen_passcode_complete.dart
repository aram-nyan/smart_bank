import 'package:flutter/material.dart';

class ScreenPasscodeComplete extends StatelessWidget {
  const ScreenPasscodeComplete({Key? key, }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
