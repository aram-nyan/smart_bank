import 'package:flutter/material.dart';

class ScreenGetStarted extends StatelessWidget {
  const ScreenGetStarted({Key? key, }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
