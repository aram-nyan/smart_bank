import 'package:flutter/material.dart';

class ScreenAboutYourself extends StatelessWidget {
  const ScreenAboutYourself({Key? key, }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
