import 'package:flutter/material.dart';

class ScreenProfileDetails extends StatelessWidget {
  const ScreenProfileDetails({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
